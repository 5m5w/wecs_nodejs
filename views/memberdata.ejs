<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>会员资料</title>
</head>
<body>
    <p>邮箱：<%= member.email %></p>
    <img src="<%= member.profilePic %>" alt="头像" style="width: 100px; height: 100px;">
    <p>Email: <%= member.email %></p>
    <p>出生年月日: <%= member.dob || '未提供' %></p>
    <p>手機號碼: <%= member.phone || '未提供' %></p>
    <p>性別: <%= member.gender || '未提供' %></p>
    <p>工作年資: <%= member.experience || '未提供' %></p>
    <p>縣市區域: <%= Array.isArray(member.location) ? member.location.join(', ') : (member.location || '未提供') %></p>
    <p>擁有的證照: <%= Array.isArray(member.certifications) ? member.certifications.join(', ') : (member.certifications || '未提供') %></p>
    <p>擅長語言: <%= Array.isArray(member.languages) ? member.languages.join(', ') : (member.languages || '未提供') %></p>
</body>
</html>